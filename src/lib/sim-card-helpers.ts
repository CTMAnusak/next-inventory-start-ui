/**
 * SIM Card Helper Functions - UI Only Version
 */

/**
 * Check if category is SIM Card
 */
export function isSIMCardSync(categoryId: string): boolean {
  return categoryId === 'cat_sim_card';
}

