# Next.js Inventory Management System - UI Only Version

## 📝 คำอธิบาย

เวอร์ชันนี้เป็น **UI-only mockup version** ของระบบจัดการคลังสินค้า สร้างด้วย Next.js  
**ไม่มีการเชื่อมต่อฐานข้อมูล** หรือ backend logic ใด ๆ ทั้งหมด  
เหมาะสำหรับ:
- การแสดง UI/UX design
- การ demo หน้าตาระบบ
- การทดสอบ layout และ component
- การนำเสนอโปรเจค

## 🎨 Features (UI Only)

### ✅ สิ่งที่ยังคงมีอยู่:
- **หน้าเพจทั้งหมด** (Pages)
- **Components ทั้งหมด** (UI components เท่านั้น)
- **Layout & Styling** (HTML, CSS, Tailwind)
- **Animations & Transitions** (Loading, Modal, Toast)
- **Drag & Scroll** (ตารางข้อมูล)
- **Pagination** (UI เท่านั้น)
- **Modal & Popup** (ทุกประเภท)
- **Forms** (UI เท่านั้น ไม่มีการ submit จริง)

### ❌ สิ่งที่ถูกลบออก:
- **MongoDB & Mongoose**
- **API Routes** (ทั้งหมด)
- **Server-side Logic**
- **Database Connections**
- **Authentication Logic** (แต่ยังมี UI ของ login/register)
- **Email Services**
- **Google OAuth** (แต่ยังมีปุ่มใน UI)
- **File Uploads** (backend part)
- **Migration Scripts**
- **Backup Scripts**

## 🚀 การติดตั้งและรันโปรเจค

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. รันโปรเจค (Development)

```bash
npm run dev
```

เปิดเบราว์เซอร์ที่: `http://localhost:3000`

### 3. Build สำหรับ Production

```bash
npm run build
npm start
```

## 📂 โครงสร้างโปรเจค

```
next-inventory/
├── src/
│   ├── app/                    # Next.js App Router pages
│   │   ├── login/             # หน้า Login (mockup)
│   │   ├── dashboard/         # หน้า Dashboard หลัก
│   │   ├── equipment-request/ # หน้าเบิกอุปกรณ์
│   │   ├── equipment-return/  # หน้าคืนอุปกรณ์
│   │   ├── it-report/         # หน้าแจ้งปัญหา IT
│   │   ├── it-tracking/       # หน้าติดตามสถานะ
│   │   ├── contact/           # หน้าติดต่อทีม IT
│   │   ├── admin/             # หน้าผู้ดูแลระบบ
│   │   │   ├── dashboard/
│   │   │   ├── inventory/
│   │   │   ├── users/
│   │   │   ├── equipment-tracking/
│   │   │   ├── equipment-reports/
│   │   │   ├── it-reports/
│   │   │   └── pending-summary/
│   │   └── ...
│   ├── components/            # React Components (UI only)
│   │   ├── Layout.tsx         # Layout หลัก
│   │   ├── Sidebar.tsx        # เมนูด้านข้าง
│   │   ├── AuthGuard.tsx      # ป้องกันหน้า (mockup)
│   │   ├── DashboardNew.tsx   # Dashboard component
│   │   └── ... (38+ components)
│   ├── contexts/              # React Contexts
│   │   ├── AuthContext.tsx    # Context สำหรับ Auth (mockup)
│   │   └── TutorialContext.tsx
│   ├── hooks/                 # Custom Hooks (UI-related only)
│   ├── lib/                   # Utility Functions
│   │   ├── mockup-data.ts     # ข้อมูล Mockup ทั้งหมด
│   │   ├── custom-toast.tsx   # Toast notifications
│   │   └── drag-scroll.ts     # Drag scrolling
│   └── middleware.ts          # Next.js Middleware (mockup)
├── public/                    # Static files
├── tailwind.config.js         # Tailwind CSS config
├── next.config.ts             # Next.js config (simplified)
└── package.json               # Dependencies (simplified)
```

## 🎭 Mockup Data

ข้อมูล mockup ทั้งหมดอยู่ใน: **`src/lib/mockup-data.ts`**

### ตัวอย่างข้อมูลที่มี:

```typescript
// ผู้ใช้งาน
mockUser           // ผู้ใช้ทั่วไป
mockAdminUser      // ผู้ดูแลระบบ

// การตั้งค่า
mockCategoryConfigs     // หมวดหมู่อุปกรณ์
mockStatusConfigs       // สถานะอุปกรณ์
mockConditionConfigs    // สภาพอุปกรณ์

// อุปกรณ์
mockOwnedItems         // อุปกรณ์ที่ใช้งาน
mockInventoryItems     // อุปกรณ์ในคลัง
mockAvailableItems     // อุปกรณ์พร้อมเบิก
mockSerialNumbers      // เลขครุภัณฑ์

// คำขอ & รายงาน
mockEquipmentRequests  // คำขอเบิกอุปกรณ์
mockITReports          // รายงานปัญหา IT
mockEquipmentTracking  // การติดตามอุปกรณ์
mockPendingSummary     // สรุปรออนุมัติ

// อื่นๆ
mockUsers              // รายชื่อผู้ใช้
mockOffices            // สำนักงาน/สาขา
mockDashboardStats     // สถิติ Dashboard
```

## 🔐 Authentication (Mockup)

ระบบ Login/Register ยังคงมี UI อยู่ แต่:
- **ไม่ตรวจสอบรหัสผ่านจริง**
- **ไม่เก็บ session จริง**
- กรอกอะไรก็ได้ แล้วกด Login จะเข้าสู่ระบบได้เสมอ
- กด "เข้าสู่ระบบด้วย Google" จะเข้าสู่ระบบได้เลย (mockup)
- กด "สมัครสมาชิกด้วย Google" จะแสดงข้อความ "รอการอนุมัติ"

### ผู้ใช้ Mockup:
- **Email อะไรก็ได้** → เข้าระบบเป็นผู้ใช้ทั่วไป
- **Email มีคำว่า "admin"** → เข้าระบบเป็น Admin
- **Password อะไรก็ได้** → ไม่มีการตรวจสอบ

## 📱 หน้าเพจที่มี

### สำหรับผู้ใช้ทั่วไป:
1. **หน้าแรก** (`/`) → redirect ไป `/dashboard`
2. **Login** (`/login`)
3. **Dashboard** (`/dashboard`) - หน้าหลักแสดงอุปกรณ์ที่ใช้งาน
4. **เบิกอุปกรณ์** (`/equipment-request`)
5. **คืนอุปกรณ์** (`/equipment-return`)
6. **แจ้งปัญหา IT** (`/it-report`)
7. **ติดตามสถานะ** (`/it-tracking`)
8. **คู่มือ IT** (`/it-manual`)
9. **ติดต่อทีม IT** (`/contact`)

### สำหรับ Admin:
1. **Admin Dashboard** (`/admin/dashboard`)
2. **จัดการคลัง** (`/admin/inventory`)
3. **จัดการผู้ใช้** (`/admin/users`)
4. **ติดตามอุปกรณ์** (`/admin/equipment-tracking`)
5. **รายงานอุปกรณ์** (`/admin/equipment-reports`)
6. **จัดการปัญหา IT** (`/admin/it-reports`)
7. **สรุปรออนุมัติ** (`/admin/pending-summary`)
8. **แก้ไขข้อมูล** (`/admin/fix-data`)

## 🎨 UI Components

### Components หลักๆ ที่มี:
- **Layout** - Layout หลักของระบบ
- **Sidebar** - เมนูด้านข้าง
- **AuthGuard** - ป้องกันการเข้าถึงหน้า (mockup)
- **DashboardNew** - Dashboard component
- **DashboardSkeleton** - Loading skeleton
- **Modals** - Modal ต่างๆ (Confirmation, Error, Loading, etc.)
- **DatePicker** - เลือกวันที่
- **SearchableSelect** - Dropdown แบบค้นหาได้
- **SerialNumberSelector** - เลือก Serial Number
- **StatusCell** - แสดงสถานะแบบสี
- **RequesterInfoForm** - ฟอร์มข้อมูลผู้ขอ
- **TutorialOverlay** - Tutorial overlay
- และอื่นๆ อีกมากมาย...

## 🎯 การใช้งาน Mockup Data

### เพิ่มข้อมูล Mockup ใหม่:

แก้ไขไฟล์ `src/lib/mockup-data.ts`:

```typescript
// เพิ่มผู้ใช้ใหม่ใน mockUsers array
export const mockUsers = [
  // ... users เดิม ...
  {
    id: 'user-5',
    email: 'newuser@example.com',
    password: '123456',
    firstName: 'ผู้ใช้',
    lastName: 'ใหม่',
    nickname: 'นิว',
    department: 'Marketing',
    phone: '0845678901',
    userType: 'individual' as const,
    office: 'สำนักงานสาขา 2',
    officeId: 'office-3',
    officeName: 'สำนักงานสาขา 2',
    isMainAdmin: false,
    userRole: 'user' as const,
    pendingDeletion: false,
  },
];
```

### ใช้ Mockup Data ในหน้าเพจ:

```typescript
import { mockUsers, mockInventoryItems, simulateApiDelay } from '@/lib/mockup-data';

// ใน Component
const [users, setUsers] = useState([]);
const [loading, setLoading] = useState(false);

const fetchUsers = async () => {
  setLoading(true);
  await simulateApiDelay(500); // จำลองความล่าช้าของ API
  setUsers(mockUsers);
  setLoading(false);
};

useEffect(() => {
  fetchUsers();
}, []);
```

## 🔧 เทคโนโลยีที่ใช้

- **Next.js 15** - React Framework
- **React 19** - UI Library
- **TypeScript** - Type Safety
- **Tailwind CSS 4** - Styling
- **Lucide React** - Icons
- **React Hot Toast** - Notifications
- **@dnd-kit** - Drag and Drop (สำหรับจัดเรียง)

## 📌 หมายเหตุสำคัญ

1. **ไม่ต้องตั้งค่า .env** - ไม่มีการเชื่อมต่อฐานข้อมูล
2. **ไม่ต้องติดตั้ง MongoDB** - ไม่มีการใช้งาน
3. **ข้อมูลจะหายเมื่อรีเฟรช** - เป็น mockup data
4. **การ Submit Form จะไม่มีผล** - เป็นการแสดง UI เท่านั้น
5. **เหมาะสำหรับ Demo เท่านั้น** - ไม่ใช่ระบบจริง

## 🐛 การแก้ไข Errors

หากเจอ error ในการ build:
1. ลบ `node_modules` และ `.next`
2. รัน `npm install` ใหม่
3. รัน `npm run dev` อีกครั้ง

```bash
rm -rf node_modules .next
npm install
npm run dev
```

## 📞 ติดต่อ

หากมีคำถามหรือต้องการความช่วยเหลือ:
- **Email**: it@vsqclinic.com
- **เบอร์โทร**: 081-134-5646
- **Line ID**: vsqitsupport

---

## 🎉 สนุกกับการใช้งาน UI Mockup!

เวอร์ชันนี้สร้างขึ้นเพื่อการแสดง UI/UX เท่านั้น  
ไม่มีการเชื่อมต่อฐานข้อมูลหรือ backend logic ใดๆ

**Made with ❤️ by CTM IT Team**

